name: Deploy Sphinx Docs to GitHub Pages

on:
  push:
    branches: ["develop"] # Update this if your default branch is 'master'
  workflow_dispatch: # Allows manual triggering

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install sphinx
          # If you use a custom theme or extensions, install them here:
          # pip install sphinx-rtd-theme
          # OR pip install -r requirements.txt

      - name: Build Sphinx documentation
        run: |
          # Compile the docs into the _build/html directory
          sphinx-build -b html docs docs/_build/html

          # Prevent GitHub Pages from ignoring folders that start with an underscore
          # Alternative is to use the 'sphinx.ext.githubpages' extension in conf.py
          touch docs/_build/html/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "docs/_build/html"

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4